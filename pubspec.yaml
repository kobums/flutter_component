name: flutter_component
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http:
  get:
  google_fonts: ^2.3.3
  shared_preferences:
  flutter_secure_storage:
  intl:
  table_calendar:
  syncfusion_flutter_datepicker:
  flutter_localizations:
    sdk: flutter
  flutter_cupertino_localizations:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/public/splash.png